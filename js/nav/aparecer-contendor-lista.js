document.addEventListener('DOMContentLoaded', function () {
    const lista = document.querySelector('.contenedor-lista .lista');

    setTimeout(function() {
        lista.style.transform = 'translateX(0)';
    }, 500); 
});
